# {{name}} — Project Context

> Generated by Genesis · {{date}}

## Overview

{{narrative}}

## Architecture

{{name}} is built with {{language}}{{#if framework}} ({{framework}}){{/if}}.
Package manager: {{packageManager}}.

## Agent Team

{{#each agents}}
### {{@index_1}}. {{agentName}}
**Domain**: {{domain.paths}} ({{domain.fileCount}} files)
**Temperature**: {{temperature}}

{{#if qualityReport.summary}}
{{qualityReport.summary}}
{{/if}}
{{#if qualityReport.bugs}}
- 🐛 {{qualityReport.bugs.length}} bugs found
{{/if}}
{{#if qualityReport.securityIssues}}
- 🔒 {{qualityReport.securityIssues.length}} security issues
{{/if}}
{{#if qualityReport.testGaps}}
- 🧪 {{qualityReport.testGaps.length}} untested files
{{/if}}

{{/each}}

## Warnings

{{#if warnings}}
{{#each warnings}}
- ⚠️ {{this}}
{{/each}}
{{else}}
None
{{/if}}

## How to Use

1. Read agent prompts in `./prompts/`
2. Switch to the appropriate agent for your task
3. Reviewer gates all changes before merge

---

*Generated by Genesis — `npx genesis .`*
